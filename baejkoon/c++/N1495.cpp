#include <iostream>
#include <queue>

using namespace std;

int main() {
    int n, s, m;
    cin >> n >> s >> m;
    int arr[n];
    for(int i=0; i<n; i++)
        cin >> arr[i];

    int answer = 0;
    bool tf = false;
    queue<pair<int, int>> q;
    q.push({0, s});

    return 0;
}